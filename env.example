# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/pcbuilder

# Database Name
DB_NAME=pcbuilder

# Optional: MongoDB Atlas connection string (if using cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pcbuilder

# Optional: Environment
NODE_ENV=development

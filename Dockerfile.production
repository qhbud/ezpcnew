# Lightweight production Dockerfile without Puppeteer dependencies
FROM node:20-slim

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install only required dependencies (skip optional puppeteer)
RUN npm ci --omit=dev --omit=optional

# Copy application files
COPY . .

# Expose port
EXPOSE 3000

# Start the application
CMD ["npm", "start"]

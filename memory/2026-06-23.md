# 2026-06-23

- Updated Fun Stats per Quinn's request:
  - Removed the visible Saved Build Comparison card.
  - Real Game FPS Estimates now shows only simple number cards for Call of Duty: Warzone and Counter-Strike 2.
  - Added `/api/builds/points`, returning anonymized saved build snapshot price/performance points.
  - Added a Price vs Performance dot plot below the FPS numbers; saved builds render as blue dots and the current selected build is highlighted in orange.
- Verification:
  - `node --check public/script.js`
  - `node --check server.js`
  - `git diff --check`
  - Puppeteer desktop/mobile checks against `http://localhost:3002`: no Saved Build Comparison text, no old FPS bars, 30 saved build dots loaded, canvas nonblank, no horizontal overflow.
